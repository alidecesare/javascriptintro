var numOne = 15;
var numTwo = 23;

if (numOne > numTwo) {
  alert("the first number is larger than the second number! good job!")
}
else if (numOne < numTwo) {
  alert("the second number is larger than the first number! you're a math whiz!")
}
else {
  alert("these are some weird symbols...what do they mean?")
}
